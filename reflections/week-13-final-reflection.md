# Final Reflection — ICS 342

**Due:** End of class, Week 13 (August 13, 2026)
**Submitted with:** Your final pull request
**Worth:** 50 points

---

This document is your final written submission for the course. It is not a summary of the syllabus. It is not a list of features you implemented. It is a record of what you actually learned, written by you, about your specific experience building this specific app this semester.

Responses that are vague, generic, or could have been written by someone who wasn't in class will not receive full credit. Responses that were generated by an AI and not substantially revised will be obvious. Write in your own voice. Be specific. Be honest.

There is no page minimum. There is a quality minimum. A response that takes two paragraphs to say something real is worth more than five paragraphs of filler.

---

## Part 1 — Your App

**1a. Open your final pull request on GitHub. Find the commit that you are most proud of — not the largest, not the last one, the one that meant the most to you. Paste the commit URL here and explain why you chose it. What did it take to get there? What was broken before and what worked after?**

> Your answer: https://github.com/Zabzar22/media-tracker-android/commit/c9a9d61d5bd12cc26bf856ccc551051bf5ec80a0
I think that I spent not only the most time on the extra credit, but learned the most about navigating the UI because of this project as well. Writing code, testing, and immediately seeing the results really helped me get a better understanding of Kotlin, and android
studio as a whole, instead of focusing on one element of code and fixing that respective piece of it. Working solo on something like this not only made it faster to learn on my end, but it made it that much more satisfying when I saw the functionality actually work.

---

**1b. Name one screen in your app that you think is genuinely well-built. Not perfect — well-built. Explain specifically why: what design decisions did you make, what did you refactor, and how does it differ from how you would have approached it in week 2?**

> Your answer: The write review screen was a fun, and well-built feature for this project. The lack of a solid picture example, like for the other screens, helped me think more creatively on how I(the user) would maneuver the page itself. The edit button for this would just mirror the
initial write-review page, making it an easy reach of the functionality whilst also being incredibly intuitive for the user, in my opinion. I also added a delete for said review not only outside of the write-review page, but added another one on the edit page incase a user
changed their mind on changing said review, and wanted to delete it instead. A thought process that I had while testing the initial functionality of the write-review itself.

---

**1c. Name one screen or feature that you are not satisfied with. What is wrong with it? If you had one more week, what specifically would you change?**

> Your answer: We never got to the account page screen, which made the entire application seemingly mock data in my mind. I would have liked to work on the account page because it makes the data seem more like live data instead of using half-live data, and half mock data.
If I had another week to work on this project, I would focus primarily on the account screen. Then the application would feel much more authentic.
---

## Part 2 — A Specific Bug

**2a. Describe the hardest bug you fixed this semester. Not the most recent one — the one that took the longest or cost you the most confusion. What was the symptom? What did you think the problem was at first? What was it actually? How did you find it?**

> Your answer: The hardest bug in my opinion, was the optimistic updates on my Library/Media Detail screens (cae99f4, https://github.com/Zabzar22/media-tracker-android/commit/c9a9d61d5bd12cc26bf856ccc551051bf5ec80a0). Before, all of the buttons like "Want To" or
"Save" would just simply grey out and sit there waiting on the backend before anything would change, and if the request failed it would just quietly flip the flag back, making it difficult to understand where the issue was.
I initially thought of changing or speeding up the loading state, but my entire approach turned out wrong. I had to make the UI update instantly and only deal with the API afterwards, so this meant keeping a backup so I could undo it if the call itself failed. 
I got the pattern mostly by copying what we built in class during week-10 and adapting it to my own ViewModels, but wasn't really sure if this was the best way of doing it or not in the long run.

---

**2b. Copy and paste the specific lines of code you changed to fix it. (This can be a before/after comparison, a diff, or just the relevant snippet.) Explain in plain English what the fix does and why it works.**

> Your answer (include code):

 // before
_uiState.value = current.copy(isUpdatingLibrary = true)
viewModelScope.launch {
    _uiState.value = try {
        val item = libraryRepository.addToLibrary(mediaId, LibraryStatus.WANT_TO)
        current.copy(libraryStatus = item.status, isUpdatingLibrary = false)
    } catch (e: Exception) {
        current.copy(isUpdatingLibrary = false)
    }
}

// after
_uiState.value = current.copy(libraryStatus = LibraryStatus.WANT_TO)
viewModelScope.launch {
    try {
        libraryRepository.addToLibrary(mediaId, LibraryStatus.WANT_TO)
    } catch (e: Exception) {
        val latest = _uiState.value as? MediaDetailUiState.Success ?: return@launch
        _uiState.value = latest.copy(libraryStatus = null)
        _actionError.value = "Couldn't add to library. Try again."
    }
}

The button initially waited on the API to respond before changing --> After, the button changes right away and the API call runs quietly behind it incase if it fails. I grab the current state and flip libraryStatus back to null, plus set an error message so it can prompt
the user. In summary, the UI basically stops waiting around and just learns how to undo itself if it guessed wrong.

---

## Part 3 — What You Actually Learned

**3a. Pick the concept from this semester that took the longest to actually understand — not just to implement, but to understand. Describe what you thought it was before you understood it, what changed, and how you would explain it now to a student who was exactly where you were at the start of the semester.**

> Your answer: I think the longest to understand for me was the optimistic updates. Most of my previous projects just used mock data like we did initially. Its difficult not just in terms of code but to actually understand why you'd even do it. 
I used to think making an api call was just as simple as using mock data. But here we need to wait, then update the screen once something actually comes back. The typical safe way tends to make everything feel slow. But once it clicked for me and realizing the screen 
can just update like it already worked, and only bother the user if it turns out it didn't, then I had a eureka moment. The part that actually gave me trouble wasn't the happy path itself, it was the concept of remembering to keep the old state around so 
I could put it back if in the case of a request failure. If I had to explain it to myself at the start of the semester, I'd probably say that "the UI is basically allowed to lie for a second as long as it owns up to it when it's wrong".

---

**3b. Your weekly reflections had a "Still Confused" section. Look back at your early reflections — weeks 1 through 4. Find something you wrote that you were confused about then. Are you still confused about it? If not, when and how did it click? If you still are, say so honestly and describe what the sticking point is.**

(If you can't access your early reflections, describe something you remember being confused about in the first half of the semester.)

> Your answer: Back in week-01 I was confused about why the app would run fine on one device but not the other, and it only started working once I cold-booted the different one. I didn't know if that meant anything or if I just got lucky to be honest. 
Looking back now, it was probably a stale build or ADB session hanging onto an old install, or just an API that wasn't compataible.  I eventually did run into similar stale build issues later in the semester, so I don't think it was just luck. Cold booting probably just 
wiped whatever cache was causing the mismatch. I didn't even think about it that way at the time though, because most of my other projects have been using a single plug and play platform. I never would have guessed the software that came built within android-studio would
have been the issue.

---

**3c. Name one thing a pod mate said, asked, or showed you during a code review or work session that changed how you approached something. It doesn't have to be a big thing. What was it, and what did it change?**

> Your answer: One thing a pod mate said that stuck with me was a comment on week-06, suggesting I consider moving data into separate collections or files if it keeps growing, since it'd be easier to maintain. It wasn't a huge thing, but it changed how I thought about 
my FakeMediaRepository file. Now, instead of just dumping everything into one big file as I added more mock data, models, and lists; I actually started thinking about splitting things out by responsibility, because even though it may not matter on an application of this scale,
any app with a large scalability will benefit from this advice even greater.

---

## Part 4 — Your Bonus Feature

**4a. Describe your bonus feature in one paragraph as if you were explaining it to someone who has never used an app before. What does it do? Why would a user want it?**

> Your answer: My bonus feature focuses on the write-review section of the application. When selecting a piece of media via search, there is a star rating that is set by the user's of the application - tied to a backend API. My feature talks to this backend API and has a star rating
and written review of the respective piece of media attached according to the user's specific rating and review. It then displays this data live to other people using the application, showing each review under the media as well as tracks the star rating(and adjusts it accordingly)

---

**4b. What was the technically hardest part of building it? Name a specific function, flow, or data structure that gave you trouble, and explain what the problem was.**

> Your answer: The most difficult part was changing the structure of mock data to live data, because I couldn't just simply assume the star rating was always going to be correct anymore. With all of the mock data I could hardcode whatever rating I wanted and it would 
just sit there static. Once it was tied to the backend API, the rating had to update the moment someone submitted a review, and actually tie back to the right media item. This means it must reflect what everyone else had rated too; not just my own submission. So, making 
sure the review write/edit flow talked to the actual DefaultReviewRepository instead of a fake list was a big hurdle, and getting the ID handling right was the part that actually broke things more than just a few times.

---

**4c. Your bonus feature has tests. Open the test file and paste the test you think is most valuable — the one that would catch the most important failure. Explain what it proves and what it does not prove.**

> Your answer (include code):
package edu.metrostate.ics342.mediatracker.ui.review

import edu.metrostate.ics342.mediatracker.data.model.Media
import edu.metrostate.ics342.mediatracker.data.model.MediaType
import edu.metrostate.ics342.mediatracker.data.model.Review
import edu.metrostate.ics342.mediatracker.data.model.UserProfile
import edu.metrostate.ics342.mediatracker.data.network.DefaultMediaRepository
import edu.metrostate.ics342.mediatracker.data.network.DefaultReviewRepository
import edu.metrostate.ics342.mediatracker.data.network.TokenStore
import io.mockk.coEvery
import io.mockk.coVerify
import io.mockk.mockk
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.ExperimentalCoroutinesApi
import kotlinx.coroutines.test.StandardTestDispatcher
import kotlinx.coroutines.test.resetMain
import kotlinx.coroutines.test.runTest
import kotlinx.coroutines.test.setMain
import org.junit.After
import org.junit.Assert.assertEquals
import org.junit.Assert.assertTrue
import org.junit.Before
import org.junit.Test
import java.io.IOException

// The ViewModel test on the submit flow that the Week 2 target asks for.
//
// The thing worth testing here is the one decision this screen makes: the same Save button
// has to POST a new review and PUT an edit, and it picks between them on its own. Both
// repositories are mockk fakes, so nothing here touches the real API.
@OptIn(ExperimentalCoroutinesApi::class)
class WriteReviewViewModelTest {

    // viewModelScope runs on Dispatchers.Main, which doesn't exist in a plain unit test,
    // so we swap in a test dispatcher we can step through by hand.
    private val dispatcher = StandardTestDispatcher()

    private val reviewRepository = mockk<DefaultReviewRepository>()
    private val mediaRepository  = mockk<DefaultMediaRepository>()

    private val media = Media(id = 1, mediaType = MediaType.BOOK, title = "Dune")

    private val myReview = Review(
        id         = 42,          // the review's own id - what PUT and DELETE need
        userId     = "user-1",
        mediaId    = 1,           // NOT the id above, and mixing them up is the classic bug
        rating     = 3,
        reviewText = "It was fine.",
        createdAt  = "2026-01-01T00:00:00Z"
    )

    @Before
    fun setUp() {
        Dispatchers.setMain(dispatcher)
        // The view model asks TokenStore who's signed in before it can ask "have I already
        // reviewed this?", so the test has to sign somebody in.
        TokenStore.currentUser = UserProfile(
            id = "user-1", email = "me@example.com",
            username = "me", displayName = "Me"
        )
        coEvery { mediaRepository.getMedia(1) } returns media
    }

    @After
    fun tearDown() {
        Dispatchers.resetMain()
        TokenStore.clear()          // or the next test inherits this login
    }

    @Test
    fun `a new review is posted`() = runTest(dispatcher) {
        coEvery { reviewRepository.getMyReview(1, "user-1") } returns null   // none yet
        coEvery { reviewRepository.createReview(1, 4, "Good.", true) } returns myReview

        val viewModel = WriteReviewViewModel(reviewRepository, mediaRepository)
        viewModel.load(1)
        dispatcher.scheduler.advanceUntilIdle()

        viewModel.onRatingChange(4)
        viewModel.onReviewTextChange("Good.")
        viewModel.submit(1)
        dispatcher.scheduler.advanceUntilIdle()

        coVerify(exactly = 1) { reviewRepository.createReview(1, 4, "Good.", true) }
        coVerify(exactly = 0) { reviewRepository.updateReview(any(), any(), any()) }
        assertTrue(viewModel.submitState.value is WriteReviewViewModel.SubmitState.Success)
    }

    @Test
    fun `an existing review is edited, and PUT gets the review id not the media id`() =
        runTest(dispatcher) {
            coEvery { reviewRepository.getMyReview(1, "user-1") } returns myReview
            coEvery { reviewRepository.updateReview(42, 5, "Better than I said.") } returns myReview

            val viewModel = WriteReviewViewModel(reviewRepository, mediaRepository)
            viewModel.load(1)
            dispatcher.scheduler.advanceUntilIdle()

            // The form filled itself in from the review that already existed - this is what
            // makes it an edit screen rather than a blank one.
            assertEquals(42, viewModel.editingReviewId.value)
            assertEquals(3, viewModel.rating.value)
            assertEquals("It was fine.", viewModel.reviewText.value)

            viewModel.onRatingChange(5)
            viewModel.onReviewTextChange("Better than I said.")
            viewModel.submit(1)
            dispatcher.scheduler.advanceUntilIdle()

            // 42, not 1. Both are ints sitting next to each other on the Review object, and
            // sending the wrong one edits somebody else's review or 404s.
            coVerify(exactly = 1) { reviewRepository.updateReview(42, 5, "Better than I said.") }
            coVerify(exactly = 0) { reviewRepository.createReview(any(), any(), any(), any()) }
            assertTrue(viewModel.submitState.value is WriteReviewViewModel.SubmitState.Success)
        }

    @Test
    fun `emptying the text box sends null so the old text is wiped`() = runTest(dispatcher) {
        coEvery { reviewRepository.getMyReview(1, "user-1") } returns myReview
        coEvery { reviewRepository.updateReview(42, 3, null) } returns myReview

        val viewModel = WriteReviewViewModel(reviewRepository, mediaRepository)
        viewModel.load(1)
        dispatcher.scheduler.advanceUntilIdle()

        viewModel.onReviewTextChange("   ")     // cleared out (the spaces get trimmed)
        viewModel.submit(1)
        dispatcher.scheduler.advanceUntilIdle()

        // null, not "". An empty string would store blank text instead of removing it.
        coVerify(exactly = 1) { reviewRepository.updateReview(42, 3, null) }
    }

    @Test
    fun `a failed save keeps you on the form so you can try again`() = runTest(dispatcher) {
        coEvery { reviewRepository.getMyReview(1, "user-1") } returns myReview
        coEvery { reviewRepository.updateReview(42, 3, "It was fine.") } throws
            IOException("network down")

        val viewModel = WriteReviewViewModel(reviewRepository, mediaRepository)
        viewModel.load(1)
        dispatcher.scheduler.advanceUntilIdle()

        viewModel.submit(1)
        dispatcher.scheduler.advanceUntilIdle()

        // Error, not Success - Success is what navigates the screen away, and nothing saved.
        assertTrue(viewModel.submitState.value is WriteReviewViewModel.SubmitState.Error)

        // clearError() runs once the snackbar has been shown, which puts the Save button
        // back in play. We're still editing the same review, so the retry is still a PUT.
        viewModel.clearError()
        assertTrue(viewModel.submitState.value is WriteReviewViewModel.SubmitState.Idle)
        assertEquals(42, viewModel.editingReviewId.value)
    }
}


I'd picked this one because it tests the exact mistake I actually made while building the screen itself. Our media ID and the review ID are both just plain ints sitting right next to each other on the Review object, and this is problematic because it's way too easy to 
grab the wrong one without anything actually looking wrong in the code. This test simply proves that when I'm editing an existing review, the submit actually calls updateReview with 42 (the review's own ID) and not 1 (the media ID), so that the createReview never gets 
touched in its path; this makes it so that it pre-fills the form from an existing review instead of just showing up blank/null. This is really what makes it feel like an edit screen instead of a second write screen, even though it is a second write screen. 
What it doesn't prove, though, is that any of this actually works once it leaves the ViewModel. All of the data is still mocked, so I don't know from this test alone whether the real API applies the update correctly or whether the screen actually navigates/re-renders 
the actual way it's supposed to once submitState flips to Success state. So the proof is that the logic is right, and not necessarily proof that the whole feature works end-to-end

---

## Part 5 — Looking Forward

**5a. If you were going to continue developing this app after the semester ends, what would you build next and why?**

> Your answer: I would build the account screen that we missed firstly, then I would move on to changing our main "my library" screen to adhere to our live data. Right now the only thing attached to live data is our search, but the application would seem much more alive
if not only the search, but the entire application was using live data instead of mock data.

---

**5b. A friend tells you they want to learn Android development. Based specifically on your experience this semester — not what you've read, what you lived — what is the one thing you would tell them to understand before they write a single line of code?**

> Your answer: Understand your software; and even moreso, understand that interoperability relies on all of the software we use. Whether that be JDK's, gradle syncs, or using the right API; if one thing is out of sync, you will more than likely be left with an empty android studio.
Understanding the ins-and-outs of not only android studio, but the software within it that works with the code is a huge hurdle for people who are used to everything being plug and play.

---

## Grading

| Section | Points |
|:---|:---:|
| Part 1 — Your App | 12 |
| Part 2 — A Specific Bug | 12 |
| Part 3 — What You Actually Learned | 14 |
| Part 4 — Your Bonus Feature | 8 |
| Part 5 — Looking Forward | 4 |
| **Total** | **50** |

Full credit in each section requires specificity — code, names, dates, conversations, actual things that happened. Half credit for responses that are genuine but stay at the surface. No credit for responses that are generic, missing, or could have been written by someone who was not in this class.
